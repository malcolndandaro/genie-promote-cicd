# Single front-door app (FastAPI + Svelte) deps — assembled into build/promote_app by
# scripts/build_promote_app.sh. (Streamlit retired at AK9; the engine-only API + AppKit at SV5.)
databricks-sdk>=0.111.0
fastapi
uvicorn
# GitHub App (bot) auth for the PR integration — mint the App JWT (RS256).
pyjwt>=2.8.0
cryptography>=42.0.0
