# Engine API (FastAPI) deps — assembled into build/engine_app by scripts/build_engine_app.sh.
# (The Streamlit app was retired at AK9; streamlit/requests are no longer needed.)
databricks-sdk>=0.111.0
fastapi
uvicorn
