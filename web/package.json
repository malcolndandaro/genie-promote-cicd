{
  "name": "genie-promote-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Svelte SPA front door for the Genie/AI-BI promotion app (served by the FastAPI engine).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "test:smoke": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "@tsconfig/svelte": "^5.0.4",
    "svelte": "^5.19.0",
    "svelte-check": "^4.1.4",
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
